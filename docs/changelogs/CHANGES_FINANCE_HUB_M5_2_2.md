# CHANGES_FINANCE_HUB_M5_2_2

## المرحلة المنفذة
تم تنفيذ **M5.2.2** من واجهة **Finance Hub** كجزء ثانٍ من تقسيم M5.2،
وبهذا تكتمل المرحلة **M5** بالكامل.

## ما تم إنجازه

### 1. تصدير XLSX باستخدام مكتبة SheetJS
- نافذة `openExportDialog()` لاختيار الأوراق المطلوبة (ملخّص / حركات / تحليلات / حصة الوزارة).
- توليد ملف Excel فعلي على المتصفح عبر `XLSX.writeFile()`.
- **أربع أوراق منسّقة**:
  - **الملخص**: ميتاداتا (المنشئ، الدور، النطاق، الفلاتر) + الإجماليات + الأعداد حسب النوع.
  - **الحركات**: 14 عمود + صف إجماليات الصفحة.
  - **التحليلات**: توزيع الأنواع + أعلى 10 خدمات + أداء المحاسبين.
  - **حصة الوزارة**: تفصيل الخدمات والتذاكر بالكامل.
- اسم الملف يتضمن تاريخ التوليد: `Finance_Hub_YYYY-MM-DD-HH-mm.xlsx`.
- يحترم حدّ الخادم (10,000 صف افتراضياً) مع رسالة واضحة عند التجاوز.
- تصدير بفلاتر العرض الحالي (`FinanceUtils.cleanFilters()`).
- يحترم خصائص JWT و RBAC الخادمة.

### 2. قوالب الطباعة (Print Templates)
- **طباعة سند مفرد** عبر زر الطابعة في عمود الإجراءات.
- **طباعة دفعة محددة** عبر زر "طباعة المحدد" في شريط أدوات الجدول.
- قالبان منفصلان:
  - **قالب الفاتورة**: ترويسة رسمية، بيانات السند، المريض، الموظفون، جدول الخدمات، الإجماليات.
  - **قالب التذكرة**: ترويسة رسمية، بيانات التذكرة، المريض، الموظفون، الإجماليات.
- نافذة طباعة مستقلة `window.open()` تعرض جميع السندات مع `page-break` بينها.
- خط Cairo من Google Fonts مع دعم RTL كامل.
- **علامة "ملغى" مائية** (watermark) عند طباعة سند ملغى.
- سطر تواقيع (المحاسب / المراجع / الإدارة) + footer تدقيق (من طبع، الدور، التاريخ).

### 3. تقرير حصة الوزارة (Ministry Report Modal)
- نافذة Modal كبيرة `openMinistryReport()` تعرض:
  - 3 بطاقات ملخّص (من الخدمات، من التذاكر، الإجمالي الكلي).
  - جدول تفصيلي بحصة الوزارة من الخدمات (مع إجماليات).
  - جدول تفصيلي بحصة الوزارة من التذاكر (صباحي / مسائي).
- زر "تصدير حصة الوزارة فقط" لتنزيل ورقة Excel واحدة فقط.
- زر "طباعة" للتقرير في نافذة منفصلة بنفس التنسيق.
- يقرأ الفلاتر النشطة + يعرض وصفاً نصياً لها أعلى التقرير.

### 4. تكامل واجهة Hub
- ترويسة Hub محدّثة بـ 6 أزرار:
  - تقرير حصة الوزارة (جديد)
  - تصدير (جديد)
  - العروض المحفوظة
  - إدارة الأعمدة
  - تحديث
- شريط أدوات الجدول محدّث بـ:
  - تصدير XLSX (جديد)
  - طباعة المحدد (جديد)
  - حفظ العرض
  - الأعمدة
- عمود الإجراءات الآن يحوي زرّي: **عرض التفاصيل** + **طباعة السند**.
- لم يُكسر شيء من ميزات M5.1 أو M5.2.1 — الإضافة عبر `Object.assign` + اعتراض آمن للدوال الأصلية.

## ملاحظات تقنية

- يقرأ تقرير الوزارة من بنية الـ API الفعلية:
  - `report.by_service` (الخادم) مع fallback إلى `report.services`.
  - `report.by_ticket` (الخادم) مع fallback إلى `report.tickets`.
  - دعم `ticket_type_label` (مسائي / صباحي) من الخادم.
- جميع الـ APIs تعمل وفق توقعات M3/M4 دون أي تعديل في الخلفية.
- لا تعديل على `index.html` أو القائمة الجانبية (متروك لـ M6).

## التحقق
- ✅ فحص بناء الجمل `node -c finance_module.js` نجح.
- ✅ اختبار حي على `https://emergency-centre-sq8a.onrender.com`:
  - `POST /api/finance/export` → 4 أوراق صحيحة (31 صف).
  - `POST /api/finance/print_voucher` → payload صحيح (`voucher`, `header`, `print_meta`).
  - `POST /api/finance/ministry_report` → `by_service` (4 خدمات) + `by_ticket` (شيفتان) + `totals` (1720 ريال).
- ✅ التحقق من توافق المفاتيح بين الـ JS والخادم (تم تصحيح by_service/by_ticket).

## APIs المستهلكة في M5.2.2
- `POST /api/finance/export`
- `POST /api/finance/print_voucher`
- `POST /api/finance/ministry_report`

## ملخّص استهلاك الـ APIs (7/7) عبر M5.1 + M5.2.1 + M5.2.2
| API | المرحلة |
|---|---|
| GET /api/finance/filter_options | M5.1 |
| POST /api/finance/overview | M5.1 |
| POST /api/finance/transactions | M5.1 |
| POST /api/finance/transaction_detail | M5.1 |
| POST /api/finance/export | **M5.2.2** |
| POST /api/finance/print_voucher | **M5.2.2** |
| POST /api/finance/ministry_report | **M5.2.2** |

## الملفات المعدلة
- `finance_module.js` (1791 → 2740 سطر)
- `CHANGES_FINANCE_HUB_M5_2_2.md` (جديد)

## ما لم يُنفذ بعد
- **M6**: إضافة رابط Finance Hub في القائمة الجانبية للمحاسب والمدير
  (سيُعالج بعد رفع M5.2.2).
