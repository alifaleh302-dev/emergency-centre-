# 🆕 التحديث: حماية النقر المزدوج + منع فتح زيارة لمريض غير مغلقة زيارته السابقة

تاريخ التحديث: 2026-05-28

## 1) حماية النظام من النقر المزدوج (Double-submit guard)

### المشكلة
عند نقر المستخدم على زر "حفظ" أو "إرسال" مرتين بسرعة قبل وصول استجابة السيرفر، كانت
البيانات تُسجّل في قاعدة البيانات مرتين (مريض مكرر، فاتورة مكررة، زيارة مكررة... إلخ).

### الحل (طبقتان متكاملتان)

**الطبقة 1 — قفل مركزي للطلبات في `main_core.js`:**
- أي طلب كتابي (POST / PUT / DELETE / PATCH) يتم تعريفه بمفتاح فريد
  `METHOD::path::JSON(data)`.
- في حال وصول طلب جديد بنفس المفتاح **بينما الطلب الأصلي ما زال قيد التنفيذ**،
  يُعاد له نفس الـ Promise مباشرة - فلا يصل لقاعدة البيانات مرتين.
- يُحرَّر القفل بعد 300ms من انتهاء الطلب الأصلي.

**الطبقة 2 — قفل بصري للأزرار `Core.guard()`:**
- يعطّل الزر ويُظهر spinner `جارٍ المعالجة...` خلال الانتظار.
- يستعيد محتوى الزر تلقائياً بعد انتهاء العملية (نجاح أو فشل).
- إن كان الزر معطّلاً بالفعل تُتجاهَل النقرات الإضافية.

### الأزرار المحمية الآن
| الواجهة | الزر | الـ Endpoint المعني |
|---|---|---|
| الطبيب | حفظ مريض جديد وفتح زيارة | `doctor/new_patient` |
| الطبيب | حفظ زيارة لمريض موجود | `doctor/existing_patient_visit` |
| الطبيب | إرسال الطلبات للأقسام | `doctor/send_orders` |
| الطبيب | حفظ وإغلاق الزيارة | `doctor/final_diagnosis` |
| المحاسبة | تأكيد السداد والطباعة | `accounting/pay_invoice` |
| الإدارة | حفظ سجل (CRUD) | `admin/save` |

> أي زر آخر في النظام يستدعي `Core.apiCall()` بطلب كتابي محمي تلقائياً عبر الطبقة المركزية - حتى لو لم يستخدم `Core.guard()` بصرياً.

---

## 2) منع فتح زيارة لمريض لم يتم إغلاق زيارته السابقة

### الوضع قبل التحديث
- ✅ القيد `uq_visits_one_active_per_patient` في PostgreSQL يمنع وجود أكثر من زيارة `Active` لنفس المريض (مطبَّق منذ migration 003 و005).
- ✅ الـ Backend في `DoctorController::existingPatientVisit` يفحص `hasActiveVisit()` ويرجع رسالة عربية واضحة.
- ❌ لكن الطبيب لم يكن يرى في نتائج البحث أن المريض لديه زيارة مفتوحة قبل أن ينقر "فتح زيارة" - فيواجه الرفض بعد فتح النموذج وملء البيانات.

### الحل
**في `models/DoctorModel.php` (`searchPatient`):**
- يُرجع الآن `has_active_visit`, `active_visit_id`, `active_visit_doctor` لكل نتيجة بحث.

**في `doctor_module.js` (`executeSearch`):**
- يظهر بجانب اسم المريض ذي الزيارة المفتوحة شارة حمراء واضحة:
  > ⚠️ زيارة مفتوحة (د. اسم الطبيب)
- يُستبدل زر "فتح زيارة" بزرّين:
  1. زر معطّل `🔒 زيارة مفتوحة` (لا يمكن فتح زيارة جديدة).
  2. زر `✓ إغلاق الزيارة السابقة` يفتح نموذج الإغلاق مباشرة.

### النتيجة
- لا يمكن للطبيب أبداً فتح زيارة جديدة لمريض لديه زيارة نشطة:
  - **الواجهة**: الزر معطل + شارة تحذيرية واضحة.
  - **الـ API**: يرفض الطلب برسالة عربية واضحة.
  - **قاعدة البيانات**: قيد فريد `uq_visits_one_active_per_patient` يمنع التكرار حتى في حالات السباق.

---

## الملفات المُعدَّلة
- `main_core.js` — قفل الطلبات المتكررة + `Core.guard()`
- `doctor_module.js` — تطبيق `Core.guard` على أزرار الحفظ + تحسين نتائج البحث
- `accounting_module.js` — تطبيق `Core.guard` على زر السداد
- `admin_module.js` — تطبيق `Core.guard` على زر الحفظ (CRUD)
- `models/DoctorModel.php` — إعادة `has_active_visit` في `searchPatient`

لا تتطلب هذه التعديلات أي migration جديد لقاعدة البيانات.
